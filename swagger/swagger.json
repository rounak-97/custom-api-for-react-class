{
  "swaggerDefinition": {
    "openapi": "3.0.0",
    "info": {
      "title": "Custom API for React Class",
      "version": "1.0.0",
      "description": "Auth & Student CRUD API Documentation by Rounak Kantha\n\n **JWT Authentication Guide**\n\n 1️ First login using **POST /api/auth/login** to get a JWT token.\n\n 2️ Click the **Authorize 🔓** button (top-right corner of this page).\n\n 3️ Enter token in this format:\n\n```\nBearer <your_jwt_token_here>\n```\n\n 4️ After authorization, you can access all 🔒 protected APIs like Profile, Create Student, Update Student, Delete Student.\n\n ⚠️ APIs with a lock icon require JWT token."
    },
    "servers": [
      {
        "url": "http://localhost:5500",
        "description": "Local server"
      },
      {
        "url": "https://custom-api-for-react-class.onrender.com/api-docs/",
        "description": "Production server (Render)"
      }
    ],
    "tags": [
      {
        "name": "Auth",
        "description": "Authentication APIs (Register, Login, Profile)"
      },
      {
        "name": "Student",
        "description": "Student CRUD APIs (JWT Protected)"
      }
    ],
    "components": {
      "securitySchemes": {
        "BearerAuth": {
          "type": "http",
          "scheme": "bearer",
          "bearerFormat": "JWT",
          "description": "Enter JWT token as: **Bearer <token>**"
        }
      }
    },
    "security": [
      {
        "BearerAuth": []
      }
    ]
  },
  "apis": [
    "./app/routes/*.js"
  ]
}