{
  "swaggerDefinition": {
    "openapi": "3.0.0",
    "info": {
      "title": "Custom API for React Class",
      "version": "1.0.0",
      "description": "Auth & Student CRUD API Documentation.\n\n🔐 Authentication:\n1. Login using /api/auth/login to get JWT token.\n2. Click the Authorize button in Swagger.\n3. Enter token as: Bearer <JWT_TOKEN>\n4. Access protected APIs (Profile, Student Create/Update/Delete)."
    },
    "servers": [
      {
        "url": "https://custom-api-for-react-class-1.onrender.com",
        "description": "Production server (Render)"
      }
    ],
    "tags": [
      {
        "name": "Auth",
        "description": "Authentication APIs"
      },
      {
        "name": "Student",
        "description": "Student CRUD APIs (JWT Protected)"
      }
    ],
    "components": {
      "securitySchemes": {
        "BearerAuth": {
          "type": "http",
          "scheme": "bearer",
          "bearerFormat": "JWT",
          "description": "Enter JWT token as: Bearer <token>"
        }
      }
    }
  },
  "apis": [
    "./app/routes/*.js"
  ]
}
